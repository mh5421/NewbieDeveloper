### 상속관계

[개발시]
1. 부모클래스의 멤버 그냥 사용
2. 부모클래스에 없는 경우 그냥 추가
3. 부모클래스에 있는데 변경이 필요한 경우(overriding)


---------------------------------------------------------------
[설계시]

- 공통요소를 부모클래스에
- 고유요소를 자식클래스에






부모와 자식간의 변수명이 겹칠때 super.변수로 표현을 하면 부모의 변수를 나타내는 완벽한 문법이 된다.

this : 자신 객체를 지칭하는 레퍼런스
super : 부모 객체를 지칭하는 레퍼런스

----------------------------------------------------
this() : 다른 생성자함수를 호출할 때
super() : 부모 생성자 함수를 호출할때 사용